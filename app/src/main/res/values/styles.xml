<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/yd_blue</item>
        <item name="colorPrimaryDark">@color/yd_navy_blue</item>
        <item name="colorAccent">@color/yd_grey</item>
        <item name="android:listDivider">@drawable/item_divider</item>
    </style>

    <!-- Base application theme. -->
    <style name="ScholarTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="TextViewProfileToolbarTitle">
        <item name="android:textStyle">bold</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:textSize">@dimen/text_size_profile_toolbar_title</item>
        <item name="android:layout_width">100dp</item>
        <item name="android:gravity">center</item>
        <item name="android:layout_height">match_parent</item>
    </style>
</resources>
